# Length of sliding window to report stats
# No impact on results
report_window_length = 300

# Length of past observations to store
# Must be greater than the window used by the system
# But apart from that has not impact.
example_window_length = 175